import SwiftUI

struct HomeView: View {
    var body: some View {
        VStack {
            Text("Home Page")
        }
        
    }
    
}

#Preview {
    HomeView()
        .frame(maxWidth: .infinity, maxHeight: .infinity)
}

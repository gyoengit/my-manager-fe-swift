import SwiftUI

struct NotFoundView: View {
    var body: some View {
        Text("Page Not Found")
    }
}

#Preview {
    NotFoundView()
        .frame(maxWidth: .infinity, maxHeight: .infinity)
}
